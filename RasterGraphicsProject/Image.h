#pragma once
#include <string>
#include <vector>
class Image
{
public:
	void load();
	void save();
	void applyTransformation();
private:
	//std::string filePath;
	//unsigned width;
	//unsigned height;
	//std::vector<std::vector<int>> data;
};

